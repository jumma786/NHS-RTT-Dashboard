# Python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Excel temp files
~$*.xlsx

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Anaconda / Jupyter workspace cruft
anaconda_projects/
.virtual_documents/

# Claude Code config (tooling only — not part of the project)
.claude/

# Large, reproducible data (regenerate from NHS source via scripts/etl.py)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
# ...except the one file the Streamlit app reads — committed so the deployed
# app (Streamlit Community Cloud) has data. The other two CSVs are only used by
# the offline Excel/Word build scripts and stay git-ignored.
!data/processed/rtt_icb_stacked.csv
